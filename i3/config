# --- Mod key ---
set $mod Mod4

# --- Font ---
font pango: Hack Nerd Font Mono 11

# The combination of xss-lock, nm-applet and pactl is a popular choice, so
# they are included here as an example. Modify as you see fit.

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
#exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork

# --- NetworkManager ---
exec --no-startup-id nm-applet

# --- Wallpaper [restore previous] ---
exec --no-startup-id nitrogen --restore

# --- Picom compositor ---
exec --no-startup-id picom

# --- Redshift nightlight [-l coords / -t screen temperature (6500k for neutral)] ---
exec --no-startup-id redshift -l -23.52:-46.35 -t 5700:3600

# Use pactl to adjust volume in PulseAudio.
# set $refresh_i3status killall -SIGUSR1 i3status
# bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
# bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
# bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
# bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

# --- Bar: Volume ---
# change volume or toggle mute
# bindsym XF86AudioRaiseVolume exec amixer -q -D pulse sset Master 5%+ && pkill -RTMIN+1 i3blocks
# bindsym XF86AudioLowerVolume exec amixer -q -D pulse sset Master 5%- && pkill -RTMIN+1 i3blocks
# bindsym XF86AudioMute exec amixer -q -D pulse sset Master toggle && pkill -RTMIN+1 i3blocks

# Use Mouse+$mod to drag floating windows to their wanted position
#floating_modifier $mod

# --- Terminal ---
bindsym $mod+Return exec alacritty

# --- Kill focused window ---
bindsym $mod+q kill

# --- Laucher ---
bindsym ctrl+$mod+space exec --no-startup-id rofi -show
bindsym $mod+space exec --no-startup-id dmenu_run

# --- Focus ---
focus_follows_mouse no

# --- Change focus ---
bindsym $mod+h focus left
bindsym $mod+Tab focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right
bindsym $mod+shift+Tab focus right

# --- Move focused window ---
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# --- Splits ---
bindsym $mod+s split h
bindsym $mod+v split v

# --- Fullscreen ---
bindsym $mod+f fullscreen toggle

# --- Change layout ---
bindsym $mod+u layout stacking
bindsym $mod+i layout tabbed
bindsym $mod+o layout toggle split

# --- Workspace names ---
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# --- Switch to workspace ---
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# --- Move focused container to workspace ---
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

# --- Reload i3 ---
bindsym $mod+Shift+c reload

# --- Restart i3 ---
bindsym $mod+Shift+r restart
 
# --- Resize mode ---
set $resize_mode Resize Mode (j and l) to grow, (h and k) to shrink or (Esc) to exit
mode "$resize_mode" {
        bindsym h resize shrink width  10 px or 10 ppt
        bindsym j resize grow   height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym l resize grow   width  10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}
bindsym $mod+r mode "$resize_mode"

# --- System mode ---
set $mode_system System ($mod + 1) logout, ($mod + 2) suspend, ($mod + 3) reboot, ($mod + 4) shutdown or (Esc) Cancel
mode "$mode_system" {
  bindsym $mod+1 exec exit; mode "default"
  bindsym $mod+2 exec systemctl suspend; mode "default"
  bindsym $mod+3 exec systemctl reboot; mode "default"
  bindsym $mod+4 exec systemctl poweroff; mode "default"
  # back to default
  bindsym Return mode "default"
  bindsym Escape mode "default"
  bindsym $mod+F4 mode "default"
}
bindsym $mod+F4 mode "$mode_system"

# --- Client Colors [Dracula palette] ---
# Class                 Border  Backgr. Text    Indicator Child_border
client.focused          #44475a #44475a #ffffff #44475a   #44475a
client.focused_inactive #333333 #5f676a #ffffff #484e50   #5f676a
client.unfocused        #333333 #222222 #888888 #292d2e   #222222
client.urgent           #2f343a #900000 #ffffff #900000   #900000
client.placeholder      #000000 #0c0c0c #ffffff #000000   #0c0c0c

client.background               #ffffff

# --- Polybar ---
#exec_always --no-startup-id $HOME/.config/polybar/launch.sh

# --- Default i3status bar ---
# bar {
#   status_command i3status
#   colors {
#     background #282a36
#     statusline #f0f1f4
#     separator #666666
#   # Class              Border     Background  Text
#     focused_workspace  #8a3ef4    #8a3ef4     #ffffff
#     active_workspace   #333333    #5f676a     #ffffff
#     inactive_workspace #363949    #363949     #999999
#     urgent_workspace   #2f343a    #900000     #ffffff
#     binding_mode       #2f343a    #900000     #ffffff
#   }
# }

# --- Bumblebee Status Bar ---
bar {
  font pango: Monospace 10
  position top
  status_command /usr/bin/bumblebee-status \
    -m  redshift pasink pasource date time
}

# --- Gaps ---
for_window [class=".*"] border pixel 3
gaps inner 13

# ---  Audio switch script ---
bindsym $mod+t exec $HOME/.config/i3/scripts/i3-sound-switcher/switch-audio-output
